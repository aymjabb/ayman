module.exports.config = {
  name: "احم",
  version: "2.0.0",
  hasPermssion: 0, // الكل يقدر يكتب الأمر بس سيرا بترد عليهم
  credits: "Ali Hussein • مطور أيمن & سيرا",
  description: "رفع المطور كمسؤول مع لمسة سيرا المستفزة ✨",
  commandCategory: "المطور",
  usages: ".احم",
  cooldowns: 5
};

module.exports.run = async ({ api, event }) => {
  const { threadID, senderID, messageID } = event;

  // معرف المطور (أيمن)
  const myUserID = '61577861540407'; 
  const decor = ["✨", "💖", "🐾", "🌟", "💫", "🎀", "🔥", "😹"];
  const randomD = () => decor[Math.floor(Math.random() * decor.length)];

  // إذا كان الشخص ليس المطور
  if (senderID !== myUserID) {
    const replies = [
      `😏 هههه ${randomD()}، تحاول تكون أدمن؟ روح العب بعيد يا شاطر 😹💫`,
      `😂 سيرا ما ترقي إلا بابا أيمن! ${randomD()}✨`,
      `🙃 كأنك نسيت نفسك؟ هذا الكرسي محجوز للمطور ${randomD()}😎`,
      `💥 اوه اوه! مين يحاول يسرق الأضواء من أيمن؟ 😹💖`,
      `😎 أيمن هو الملك هنا، لا تحاول التقليد ${randomD()}🔥`,
      `🐾 سيرا تقول لك: "نوم العوافي، أحلامك كبيرة مرة" 💤✨`
    ];
    const reply = replies[Math.floor(Math.random() * replies.length)];
    return api.sendMessage(reply, threadID, messageID);
  }

  // إذا كان المنفذ هو المطور (أيمن)
  try {
    api.sendMessage(`╭──── • ◈ • ────╮\n  أوامـرك مـطـاعة يا مـلـكي ✨\n╰──── • ◈ • ────╯\n\n🐾 جاري رفعك أدمن.. استعد للهيبة! ${randomD()}`, threadID, async () => {
      await api.changeAdminStatus(threadID, myUserID, true);
      api.sendMessage(`✨ تداااااا! أيمن صار المسؤول الحين 🥳🔥\nويل للي يرفع عينه فيه! 🎀`, threadID);
    }, messageID);
  } catch (e) {
    api.sendMessage(`🥺 سيرا حاولت ترفعك بس شكل البوت مو أدمن أو فيه مشكلة!`, threadID, messageID);
  }
};
